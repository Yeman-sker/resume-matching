# 简历-岗位匹配系统 - 领域术语表

## 核心概念

### 简历（Resume）
求职者提交的个人信息，包含技能、学历、经验、期望薪资等维度。

### 岗位（Job）
企业发布的招聘需求，包含必备技能、加分技能、学历要求、经验要求等。

### 匹配（Match）
计算简历与岗位的适配程度，输出多维度分数和推荐理由。

## 批处理任务（Batch Job）

一次完整的「模型训练 + 匹配计算」流程：读取全量清洗后的简历和岗位数据，训练 TF-IDF 和 Word2Vec 模型并保存为带版本号的新模型，随后用刚训练的模型对全量简历×岗位计算匹配分数，覆盖旧的匹配结果。

训练和匹配总是作为一个整体执行，不能单独触发。触发方式有两种：定时自动触发、手动触发，两者执行的是同一个任务。

## 评分体系

### 语义分（Semantic Score）
通过文本语义分析计算简历与岗位的相关性，范围 0-100。

**计算公式：**
```
语义分 = TF-IDF 分 × 60% + Word2Vec 分 × 40%
```

**子维度：**
- **TF-IDF 分**：基于词频-逆文档频率的余弦相似度，反映关键词匹配程度
- **Word2Vec 分**：基于词向量的余弦相似度，反映语义相关性（如"Python"和"编程"的关联）

**相似度转分数：** 余弦相似度范围 [-1, 1]，转换公式 `(similarity + 1) / 2 × 100`

### 技能分（Skill Score）
评估简历技能与岗位要求的匹配程度，范围 0-100。

**计算公式：**
```
技能分 = 必备技能匹配率 × 85 + 加分技能匹配个数 × 5（上限 +15）
```

**相关字段：**
- **matched_skills**：简历与岗位的共同技能（用管道符 `|` 分隔）
- **missing_skills**：岗位必备但简历缺少的技能（用管道符 `|` 分隔）

### 学历分（Education Score）
评估学历是否满足岗位要求，范围 0-100。
- 满足要求：100
- 差 1 级：60
- 差 2 级及以上：30

### 经验分（Experience Score）
评估工作年限是否满足岗位要求，范围 0-100。
- 满足要求：100
- 差 1 年：70
- 差 2 年：40
- 差 3 年及以上：20

### 城市分（City Score）
评估工作地点匹配程度，范围 0-100。
- 城市匹配：100
- 城市不匹配：50
- 城市未知：60

### 薪资分（Salary Score）
评估期望薪资与岗位薪资的匹配程度，范围 0-100。
- 期望薪资 ≤ 岗位上限：100
- 岗位上限 ≥ 期望薪资 × 0.8：70
- 其他：40
- 薪资未知：60

### 证书分（Certificate Score）
评估是否持有相关证书，范围 0-100。
- 有证书：100
- 无证书：60

### 总分（Total Score）
7 个维度的加权和，范围 0-100。

**计算公式：**
```
总分 = 语义分 × 60% + 规则分 × 40%
规则分 = 技能分 × 40% + 学历分 × 20% + 经验分 × 15%
       + 城市分 × 10% + 薪资分 × 10% + 证书分 × 5%
```

## 数据字段

### tokens
分词后的词列表，用于文本语义分析。
- **存储格式**：JSON 数组字符串，如 `["Python", "数据分析", "机器学习"]`
- **使用场景**：TF-IDF 和 Word2Vec 模型训练和转换

### clean_text
分词后用空格连接的文本，用于 TF-IDF 向量化（当前已被 tokens 替代）。

### standard_skills / required_skills_standard / preferred_skills_standard
标准化后的技能列表。
- **存储格式**：管道符 `|` 分隔，如 `Python|SQL|Spark`
- **使用场景**：技能分计算

## 匹配结果（Match Result）

一条匹配记录包含以下字段：
- **标识字段**：resume_id, resume_name, job_id, job_title, department
- **语义分数**：tfidf_score, word2vec_score, semantic_score
- **规则分数**：skill_score, education_score, experience_score, city_score, salary_score, certificate_score, rule_score
- **总分**：total_score
- **技能详情**：matched_skills（共同技能），missing_skills（缺失技能）
- **推荐理由**：reason（综合描述各维度的匹配情况）

## 模型

### TF-IDF 模型
通过 `CountVectorizer + IDF` 训练，把文本转换为 TF-IDF 向量。
- **词表大小**：最多 500 个词
- **最小文档频率**：至少在 2 个文档中出现
- **输入**：tokens 列（`Array[String]`）
- **输出**：TF-IDF 向量（`Vector` 类型）

### Word2Vec 模型
学习词的语义向量表示，通过平均词向量表示整句话。
- **向量维度**：100
- **窗口大小**：5
- **最小词频**：1
- **训练轮数**：30
- **输入**：tokens 列（`Array[String]`）
- **输出**：句子向量（`Vector` 类型，通过平均词向量得到）
